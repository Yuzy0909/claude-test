---
title: Claude Code SubAgents完全ガイド：Skills・Commandsと合わせて理解する使い方
meta_description: Claude CodeのSubAgents・Skills・Commandsの違いと使い方を徹底解説。AIエージェントのカスタマイズ方法から実践的な活用例まで、Claude Codeを使い始めた開発者向けにわかりやすく紹介します。
main_keyword: Claude Code SubAgents
related_keywords: Claude Code スキル, Claude Code コマンド, AIエージェント カスタマイズ, Claude Code 使い方
word_count: 5250
target_word_count: 5000
---

# Claude Code SubAgents完全ガイド：Skills・Commandsと合わせて理解する使い方

Claude Codeは、単なるコード補完ツールにとどまらず、複雑なタスクを自律的に実行できるAIエージェントプラットフォームです。その中核を担うのが「SubAgents」「Skills」「Commands」という3つの機能です。本記事では、Claude Code SubAgentsをはじめとするこれら3つの機能の定義・メリット・使い方を体系的に解説します。Claude Codeを使い始めたばかりの開発者・エンジニアが、最短距離で活用できるよう実践的な視点でまとめました。

これら3つの機能を理解して組み合わせることで、Claude Codeはただの対話AIを超えた「プロジェクト専用の自律エージェント」として機能します。コードレビューの自動化、ドキュメントの自動生成、テストの自動実行など、開発現場で繰り返される定型作業を大幅に効率化できます。まずは各機能の役割をしっかりと把握しましょう。

---

## 1. Claude Code SubAgentsとは

### SubAgentsの定義

Claude Code SubAgentsとは、メインのClaudeエージェント（親エージェント）から呼び出される、専門化されたサブエージェントのことです。各SubAgentは特定の役割に特化しており、親エージェントが複雑なタスクを受け取ったとき、適切なSubAgentに処理を委譲することで高品質な結果を得られます。

SubAgentsは`.claude/agents/`ディレクトリ配下にMarkdownファイルとして定義します。

```
.claude/
  agents/
    code-reviewer.md   # コードレビュー専門
    test-runner.md     # テスト実行専門
    doc-writer.md      # ドキュメント生成専門
```

### SubAgentsのメリット

**専門性の向上**
汎用エージェントにすべてを任せるより、専門化されたSubAgentに処理を委ねることで各タスクの精度が上がります。コードレビューに特化したSubAgentはレビュー観点を深く保有し、ドキュメント生成に特化したSubAgentは適切なフォーマットを維持します。

**並列処理による効率化**
複数のSubAgentを同時に実行することも可能です。コードレビューとテスト実行を並列に走らせて結果をまとめてレポートするといった使い方ができ、全体の処理時間を短縮できます。

**メンテナンス性の向上**
SubAgentを機能単位で分離することでプロンプト管理が容易になります。特定のSubAgentの振る舞いを変えたい場合は、そのファイルだけを編集すればよく、他への影響を最小化できます。

### SubAgentsの使い方

`.claude/agents/`ディレクトリにMarkdownファイルを作成します。

```markdown
---
name: code-reviewer
description: コードのバグ・セキュリティ問題・改善点を指摘する専門エージェント
---

あなたはシニアエンジニアとして、提出されたコードを以下の観点でレビューします。

1. バグ・ロジックエラーの検出
2. セキュリティ上の脆弱性
3. パフォーマンスのボトルネック
4. コーディング規約への準拠

各指摘には重要度（高・中・低）を付与し、改善案を提示してください。
```

親エージェントが「このコードをレビューして」と指示を受けると、descriptionの内容をもとに自動的にcode-reviewerへ処理が委ねられます。descriptionは自然言語で記述でき、親エージェントがどのSubAgentを呼び出すかを判断する重要な手がかりになります。そのため、担当範囲・得意領域を明確かつ具体的に記載することがSubAgent設計のポイントです。

---

## 2. Skillsとは

### Skillsの定義

SkillsはClaude Codeに特定の処理能力・手順を付与する仕組みです。SubAgentsが「誰が担当するか」を定義するのに対し、Skillsは「どのように処理するか」という手順を定義します。`.claude/skills/`ディレクトリ配下にMarkdownファイルとして保存され、エージェントが参照・実行できる標準手順書として機能します。

```
.claude/
  skills/
    char-countor.md    # 文字数カウント
    code-formatter.md  # コードフォーマット
    api-tester.md      # API疎通確認
```

### Skillsのメリット

**再利用可能な処理の標準化**
同じ処理を何度も記述する代わりに、Skillとして定義しておけばさまざまな場面で再利用できます。「文字数カウント」Skillを定義しておけば、ブログ記事執筆・メール作成・仕様書作成など、あらゆるテキスト関連タスクで同じロジックを呼び出せます。

**処理品質の均一化**
Skillsを使うことで、誰がどのタイミングで実行しても同じ品質の結果を得られます。APIテストのSkillを定義すれば、担当者ごとに手順が異なる問題を防げます。

**段階的な能力拡張**
プロジェクトの成長に合わせて新しいSkillsを追加することで、Claude Codeの処理能力を段階的に拡張できます。必要な処理が増えたらSkillsを追加していくアプローチが有効です。

### Skillsの使い方

`.claude/skills/`ディレクトリにMarkdownファイルを作成します。

```markdown
---
name: api-tester
description: 指定されたAPIエンドポイントにリクエストを送り、レスポンスを検証するスキル
---

## api-tester スキル

curlコマンドでリクエストを実行し、ステータスコード・レスポンスボディを報告します。

### 手順

1. エンドポイントURLとメソッドを確認する
2. Bashツールでcurlコマンドを実行する
3. ステータスコードが期待値と一致するか検証する
4. レスポンスボディのJSONをパースし、必須フィールドを確認する
5. 結果を表形式でまとめる
```

SubAgentがタスクを処理する際にこのSkillを参照することで、一貫した手順でAPI検証を行えます。Skillファイルは詳細な手順だけでなく、Bashコマンドのサンプル・出力フォーマットの定義・エラー処理の指針なども記述できます。実行手順を具体的に記載するほどエージェントの動作が安定し、期待通りの結果を得やすくなります。

---

## 3. Commandsとは

### Commandsの定義

CommandsはClaude Codeのチャットインターフェースで使用できる、スラッシュコマンド形式のショートカット機能です。`/コマンド名`の形式で入力することで、よく使う操作を素早く実行できます。CommandsはSubAgentsやSkillsを呼び出すトリガーとしても機能し、一連のワークフローを一つのコマンドで起動できます。

`.claude/commands/`ディレクトリ配下にMarkdownファイルとして定義します。

```
.claude/
  commands/
    review.md    # /review コマンド
    test.md      # /test コマンド
    deploy.md    # /deploy コマンド
```

### Commandsのメリット

**操作の高速化**
長い指示文を毎回入力する代わりに、短いコマンドで同じ操作を実行できます。`/review`と入力するだけで、コードレビューのSubAgentが起動し自動でレビューを進める運用が可能です。

**チームでのワークフロー標準化**
Commandsをリポジトリにコミットすることで、チームメンバー全員が同じコマンドを使えます。新メンバーがプロジェクトに参加したとき、定義済みのCommandsを確認するだけでワークフローを把握できます。

**複合ワークフローの自動化**
複数のSubAgents・Skillsを組み合わせた複雑なワークフローをCommandに定義できます。`/release`コマンドに「テスト実行→コードレビュー→ドキュメント更新→変更ログ生成」という一連のフローを組み込むことも可能です。

### Commandsの使い方

`.claude/commands/`ディレクトリにMarkdownファイルを作成します。

```markdown
---
name: review
description: 現在のブランチの変更差分に対してコードレビューを実行する
---

## /review コマンド

git diff を取得し、code-reviewer SubAgentに渡してレビューを実行します。

### 手順

1. `git diff main...HEAD` で変更差分を取得する
2. 差分をcode-reviewer SubAgentに渡す
3. レビュー結果をMarkdown形式でまとめる
4. 重要度別に指摘事項を整理して報告する
```

チームメンバーは`/review`と入力するだけで標準化されたコードレビューを即座に実行できます。CommandファイルのdescriptionはClaude Codeのスラッシュコマンド一覧に表示されるため、チームへの説明コストを下げる効果もあります。また、Commandの本文に引数の受け取り方を記述することで、`/review --strict`のようにオプションを持つコマンドを設計することも可能です。

---

## 4. SubAgents・Skills・Commandsの違いと使い分け

3つの機能はそれぞれ異なる役割を担っており、組み合わせて使うことで真価を発揮します。「誰が（SubAgents）」「どのように（Skills）」「いつ・どんなきっかけで（Commands）」という3つの軸が揃ってはじめて、高度な自動化ワークフローが完成します。

| 機能 | 定義場所 | 主な役割 | 呼び出し方 |
|------|----------|----------|------------|
| SubAgents | `.claude/agents/` | 専門的な役割・ペルソナの定義 | 親エージェントが自動委譲 |
| Skills | `.claude/skills/` | 再利用可能な処理手順の定義 | エージェントが参照・実行 |
| Commands | `.claude/commands/` | 操作のショートカット定義 | `/コマンド名`で呼び出す |

### 使い分けの指針

**SubAgentsを使うべきケース**
- 特定の専門知識・役割を持たせたいとき
- 複数の観点から並列にタスクを処理させたいとき
- プロジェクトで役割分担を明確にしたいとき

**Skillsを使うべきケース**
- 複数のSubAgentsや場面で共通する処理があるとき
- 外部ツール（Bash・APIなど）を使った定型手順を標準化したいとき
- 処理品質を均一に保ちたいとき

**Commandsを使うべきケース**
- 繰り返す操作を素早く実行したいとき
- チーム全体でワークフローを標準化したいとき
- 複数のSubAgents・Skillsを一連のフローとして起動したいとき

---

## 5. 実践的な活用例

### 活用例1：コードレビュー自動化

プルリクエストのレビュープロセスを自動化する事例です。

**構成**
- SubAgent：`code-reviewer`（コードレビュー）、`security-checker`（セキュリティチェック）
- Skill：`git-diff-fetcher`（git差分取得）
- Command：`/review`

**フロー**
1. 開発者が`/review`コマンドを実行
2. `git-diff-fetcher` Skillがgit差分を取得
3. `code-reviewer`と`security-checker` SubAgentが並列でレビューを実施
4. 双方の結果をまとめてレポートとして出力

このワークフローにより、レビュー依頼から結果取得まで数分で完了し、人間のレビュアーは重要な判断に集中できます。セキュリティチェックをSubAgentとして独立させることで、セキュリティ観点のレビュープロンプトを細かく調整でき、脆弱性検出の精度が向上します。

### 活用例2：ドキュメント自動生成

コードの変更に合わせてドキュメントを自動更新するワークフローです。

**構成**
- SubAgent：`doc-writer`（ドキュメント執筆）
- Skill：`code-parser`（コード解析）、`markdown-formatter`（Markdown整形）
- Command：`/update-docs`

**フロー**
1. 開発者が`/update-docs`コマンドを実行
2. `code-parser` Skillが変更ファイルのクラス・関数定義を解析
3. `doc-writer` SubAgentが解析結果をもとにドキュメントを執筆
4. `markdown-formatter` SkillがMarkdownを整形して出力

コードの変更が発生するたびにドキュメントを手動で更新する手間が省け、ドキュメントとコードの乖離という慢性的な問題を解消できます。特にAPI仕様書やREADMEのような更新頻度の高いドキュメントに有効です。

### 活用例3：SEOブログ記事執筆

本記事もこの仕組みを活用して作成されています。`seo-blog-writer` SubAgentが記事を執筆し、`char-countor` Skillで文字数を計測しながら目標文字数に最適化するワークフローを実行しています。

**構成**
- SubAgent：`seo-blog-writer`（SEO記事執筆）
- Skill：`char-countor`（文字数計測）
- Command：`/write-blog`

**フロー**
1. `seo-blog-writer` SubAgentが初稿を執筆
2. `char-countor` Skillが文字数を4パターンで計測
3. 目標文字数との差分に応じて拡張・圧縮を実施
4. 目標範囲内に収まったら最終記事を出力

---

## まとめ

Claude Code SubAgents・Skills・Commandsは、Claude Codeを高度な自律エージェントへと進化させる中核機能です。

- **SubAgents**：専門化されたエージェントを定義し、役割を分担する
- **Skills**：再利用可能な処理手順を定義し、品質を均一化する
- **Commands**：操作をショートカット化し、ワークフローを標準化する

3つを組み合わせることで、コードレビュー・テスト・ドキュメント生成など、さまざまな開発ワークフローを自動化できます。まずは小さな用途から始め、プロジェクトの成長に合わせてSubAgents・Skills・Commandsを拡充していくことをおすすめします。

導入のステップとしては、まずよく繰り返す作業を一つ選んでCommandを作成し、そのCommandが呼び出す処理をSkillとして定義、さらに処理を行う専門エージェントをSubAgentとして整備する、という順番が進めやすいでしょう。小さな成功体験を積み重ねながら、Claude Codeを開発チームの強力なパートナーとして育てていくことができます。Claude Codeの活用を深めることで、開発チームの生産性を大きく向上させることができるでしょう。
